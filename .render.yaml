services:
  - type: web
    name: expense-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python expense_bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
